module Main (
  main
) where

import Types
import Network.Types

--packRequest :: InputMessage -> RequestMessage

main = do
    return()
--open connection
--get arguments
--transform to requestmessage
--send messages
